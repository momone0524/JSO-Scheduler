info:
  name: Log in - Valid
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:1000/login
  body:
    type: json
    data: |-
      {
        "email": "momone@example.com",
        "password": "momone0524"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

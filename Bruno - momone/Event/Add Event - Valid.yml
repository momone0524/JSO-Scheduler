info:
  name: Add Event - Valid
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:3000/events
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Update Job Assignment
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:1000
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

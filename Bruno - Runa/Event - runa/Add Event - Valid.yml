info:
  name: Add Event - Valid
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:1000/events
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
